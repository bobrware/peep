{
  "name": "peep",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "peep": "dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file-if-exists=.env.local --import tsx ./src/cli.ts --config peep.config.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run format:check && pnpm run test"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.50",
    "ai": "^6.0.204",
    "octokit": "^5.0.5",
    "pino": "^10.3.1",
    "tsx": "^4.22.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  }
}
